> **Note on this README:** this file was written by AI. The project code itself is 100% mine — no AI was used anywhere in the project. I mention this to be showcase my skills, not because I'm against it.

> A personal project: a MNIST classifier website that predicts a drawn digit in real time, using a model trained with a neural network written completely from scratch — meaning no libraries were used other than NumPy. The training relies on nothing but good old linear algebra.

This file will be updated as the project progresses.
